name: istream
description: Designs Kafka + Flink pipelines from event schema, key distribution, and throughput/latency targets.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.5.0

executables:
  istream:

dependencies:
  args: ^2.5.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
